
// Fog------
float4 _FogDistances : _FogDistances : register(vs_1_1, c93);

float Calc_Fog(float W)
{
	// ((-1/Range)*d)+End/Range = (End-d)/Range
	return ((_FogDistances.y - W) / (_FogDistances.y - _FogDistances.x));
}
//----------
